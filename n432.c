#include <stdio.h>
#include <string.h>

int main(void)
{
    int T;
    scanf("%d", &T);

    char S[1000];
    for(int i=0; i < T; i++)
    {
        scanf("%s", S);
        for(int j=0; j < strlen(S); j++)
        {
            for(int k=0; k < strlen(S)-j; k++)
            {

            }
        }

        S = "";
    }

    return 0;
}
